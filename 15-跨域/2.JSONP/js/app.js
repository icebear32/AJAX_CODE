const data = {
    name: 'ich'
};

// console.log(data);

// 处理数据
// function handle(data) {
//     // 获取 result 元素
//     const result = document.getElementById('result');
//     result.innerHTML = data.name;
// }

handle(data);